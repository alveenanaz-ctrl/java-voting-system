# java-voting-system
A console-based Voting System developed in Java that allows admin to manage candidates and voters to register, login, and cast votes. The system uses file handling to permanently store candidates, votes, and voter information, preventing duplicate voting and ensuring data persistence.
